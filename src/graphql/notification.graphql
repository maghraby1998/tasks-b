type Query {
  notifications: [Notification!]!
  notification(id: ID!): Notification
}

type Notification {
  id: ID!
  title: String!
  message: String!
  user: User
  isRead: Boolean!
  created_at: String!
}
